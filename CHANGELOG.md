# Changelog

All notable changes to this project will be documented in this file.

## [0.1.2] — 2025-02-01

### Features

- Add custom provider account switching
- Add account management commands (`/accounts:add`, `/accounts:edit`, `/accounts:remove`, `/accounts:switch`, `/accounts:oauth`)
- Add provider management commands (`/providers:add`, `/providers:edit`, `/providers:remove`)
- Add model management commands (`/models:list`, `/models:add`, `/models:remove`)
- Add system reset command (`/system:reset`)

### Miscellaneous

- Add MIT license
- Add npm publish workflow
- Bump version to 0.1.2
